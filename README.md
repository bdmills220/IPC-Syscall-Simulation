# IPC-Syscall-Simulation
A Windows-based OS project that simulates kernel and user mode interactions using Interprocess Communication (IPC) with Named Pipes. The user process sends syscall requests to a kernel process, which dispatches operations through a jump table and returns results, demonstrating core OS concepts and client-server communication.
